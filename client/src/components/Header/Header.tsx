import './Header.scss';

function Header() {
    return (
        <header className='site-header'>
            <nav>
                <a href="#">TEST HEADER</a>
            </nav>
        </header>
    );
}

export default Header;